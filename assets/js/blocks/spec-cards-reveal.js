/**
 * Карточки `.isg-spec-cards` в product intro.
 * Ранее: scroll + GSAP (y / rotateX / scale) — карточки визуально «спрыгивали» при скролле.
 * Сейчас: без трансформаций; кегль и поток только из CSS.
 */
export function initSpecCardsReveal() {
  return () => {};
}
